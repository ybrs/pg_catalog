[pytest]
# Pin the rootdir to this directory and put it on sys.path so tests can import
# project-root helper modules (e.g. validate_pg_catalog_views) and so sibling
# test modules import by bare name — regardless of how pytest is invoked
# (`pytest tests`, `pytest`, or `python -m pytest`) or from which directory.
pythonpath = .
testpaths = tests
